<% if @returns_to_ho.errors.full_messages.present? %>
  $("#notification").html("<%= j(render 'layouts/errors') %>");
  window.scrollTo(0, 0);
<% else %>
  window.location = '/returns_to_hos'
<% end %>