<% @purchase_transfer.product_mutation_details.group_by{|prd|prd.product_size.product_id}.each do |prd| %>
  <% detail = prd[1][0].product_size %>
  <% product = detail.product %>
  <%= render 'tr_detail', product: product %>
<% end %>
