<% if @purchase_transfer.errors.full_messages.present? %>
  $("#notification").html("<%= j(render 'layouts/errors') %>");
  window.scrollTo(0, 0);
<% else %>
  window.location = '/purchase_transfers'
<% end %>
