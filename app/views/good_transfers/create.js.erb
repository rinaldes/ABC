<% if @good_transfer.errors.full_messages.present? %>
  $("#notification").html("<%= j(render 'layouts/errors') %>");
  window.scrollTo(0, 0);
<% else %>
  window.location = '/good_transfers'
<% end %>