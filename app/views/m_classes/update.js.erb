<% if @errors.present? %>
  $("#notification").html("<%= j(render 'layouts/errors') %>");
  window.scrollTo(0, 0);
<% else %>
  <% flash[:notice] = "Successfully updated" %>
  window.location = '/m_classes'
<% end %>
