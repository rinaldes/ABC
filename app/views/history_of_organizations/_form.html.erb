<% if params[:action] == "new"%>
	<tr>
<% end %>
	<%= form_for(history_of_organization, html: { role: "form", multipart: true}, remote: true) do |f| %>
		<td>
			<div class="checkbox check-default">
                <input id="checkbox12" type="checkbox" value="1">
                <label for="checkbox12"></label>
            </div>
		</td>

		<td>
			<%= f.text_field :name, :class => "full-width", :placeholder => "Name" %>
		</td>

		<td>
			<%= f.text_field :phone_number, :class => "full-width", :placeholder => "Phone Number" %>
		</td>

		<td>
			<%= f.select :position, options_for_select(position) %>
			<% if params[:action] == "new"%>
				<%= f.hidden_field :employee_id, :value => params[:employee_id]%>
			<% else %>
				<%= f.hidden_field :id, :value => params[:id] %>
			<% end %>
		</td>

		<td>
			<%= f.text_field :statue, :class => "full-width", :placeholder => "Statue" %>
		</td>

		<td>
			<div class="input-append success date">
                <%= f.text_field :start, :class => "form-control"%>
                <span class="add-on"><span class="arrow"></span><i class="fa fa-th"></i></span> 
            </div>
		</td>

		<td>
			<div class="input-append success date">
                <%= f.text_field :end, :class => "form-control"%>
                <span class="add-on"><span class="arrow"></span><i class="fa fa-th"></i></span> 
            </div>
		</td>

		<td>
			<div>
				<% if params[:action] == "new" %>
            		<%= f.submit "Add", class: "btn btn-success", :style => "margin: 0 30px;", :onclick => "submit_history_of_organization();"%>
            	<% else %>
            		<%= f.submit "Update", class: "btn btn-success", :style => "margin: 0 30px;", :onclick => "update_history_of_organization();"%>
            	<% end %>
            </div>
		</td>
	<% end %>
<% if params[:action] == "new"%>
	</tr>
<% end %>