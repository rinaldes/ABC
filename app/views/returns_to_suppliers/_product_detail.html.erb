<% @receiving.receiving_details.group_by{|prd|prd.product_size.product_id}.each do |prd| %>
  <% product = prd[1][0].product_size.product %>
  <%= render partial: 'modal_product', locals: {product: product, quantities: [], product_sizes: product.product_sizes, key: "#{key}#{product.id}"} %>
<% end %>
