<thead>
  <tr>
    <th style="vertical-align: middle;" rowspan=2>No</th>
    <th style="vertical-align: middle;" rowspan=2>Bulan</th>
    <th colspan=2>Realisasi <%= params[:year].present? ? (params[:year].to_i - 4) : (Time.now.year - 4) %></th>
    <th colspan=2>Realisasi <%= params[:year].present? ? (params[:year].to_i - 3) : (Time.now.year - 3) %></th>
    <th colspan=2>Realisasi <%= params[:year].present? ? (params[:year].to_i - 2) : (Time.now.year - 2) %></th>
    <th colspan=3>Realisasi <%= params[:year].present? ? (params[:year].to_i - 1) : (Time.now.year - 1) %></th>
    <th colspan=2>Realisasi Grow Up <%= params[:year].present? ? (params[:year].to_i - 1) : (Time.now.year - 1) %></th>
    <th colspan=2>Rencana Target <%= params[:year].present? ? (params[:year].to_i) : (Time.now.year) %></th>
    <th colspan=3>Realisasi Target <%= params[:year].present? ? (params[:year].to_i) : (Time.now.year) %></th>
    <th colspan=3>Rencana Grow Up <%= params[:year].present? ? (params[:year].to_i) : (Time.now.year) %></th>
    <th colspan=3>Realisasi Grow Up <%= params[:year].present? ? (params[:year].to_i) : (Time.now.year) %></th>
  </tr>
  <tr>
    <% num_a = [4, 9, 8, 7] %>
    <% (1..9).each do |num| %>
      <th>QTY</th>
      <th>Value</th>
      <% if (num_a & [num]).present? %>
        <th>%</th>
      <% end %>
    <% end %>
  </tr>
</thead>
<tbody>
  <% @bulan.each_with_index do |bulan, i| %>
    <% if i == 6 %>
      <tr>
        <th colspan="2">Total Periode I</th>
        <!-- Dummy -->
          <% num_a = [4, 9, 8, 7] %>
          <% (1..9).each do |num| %>
            <th style="text-align: right;"><%= rand(10000) %></th>
            <th style="text-align: right;"><%= format_currency rand(1000000000) %></th>
            <% if (num_a & [num]).present? %>
              <th style="text-align: right;"><%= rand(100) %>%</th>
            <% end %>
          <% end %>
        <!-- Dummy End -->
      </tr>
    <% end %>
    <tr>
      <td><b><%= i+1 %></b></td>
      <td><b><%= bulan %></b></td>
      <!-- Dummy -->
        <% num_a = [4, 9, 8, 7] %>
        <% (1..9).each do |num| %>
          <td style="text-align: right;"><%= rand(10000) %></td>
          <td style="text-align: right;"><%= format_currency rand(1000000000) %></td>
          <% if (num_a & [num]).present? %>
            <td style="text-align: right;"><%= rand(100) %>%</td>
          <% end %>
        <% end %>
      <!-- Dummy End -->
    </tr>
  <% end %>
  <tr>
    <th colspan="2">Total Periode II</th>
    <!-- Dummy -->
      <% num_a = [4, 9, 8, 7] %>
      <% (1..9).each do |num| %>
        <th style="text-align: right;"><%= rand(10000) %></th>
        <th style="text-align: right;"><%= format_currency rand(1000000000) %></th>
        <% if (num_a & [num]).present? %>
          <th style="text-align: right;"><%= rand(100) %>%</th>
        <% end %>
      <% end %>
    <!-- Dummy End -->
  </tr>
  <tr><td> </td></tr>
  <tr>
    <th colspan="2">Grand Total</th>
    <!-- Dummy -->
      <% num_a = [4, 9, 8, 7] %>
      <% (1..9).each do |num| %>
        <th style="text-align: right;"><%= rand(10000) %></th>
        <th style="text-align: right;"><%= format_currency rand(1000000000) %></th>
        <% if (num_a & [num]).present? %>
          <th style="text-align: right;"><%= rand(100) %>%</th>
        <% end %>
      <% end %>
    <!-- Dummy End -->
  </tr>
  <tr>
  <td colspan="18"></td>
  <th colspan="3"><%= rand(100) %>%</th>
  <td colspan="3"></td>
  </tr>
</tbody>